/*La sentencia if(...) evalúa la condición de los paréntesis. Si el resultado es 
true ejecuta un bloque de código.
Si queremos ejecutar cierto código cuando la condición no se cumple, 
podemos concatenar el if con la sentencia else.
También podemos concatenar if’s con la sentencia else if.
*/

Sintaxis
if (condition) {
  //  bloque de codigo a ejecutar, si la ejecucion es verdadera 
}

//Tenga en cuenta que está en minúsculas. Las letras mayúsculas (If o IF) generarán un error de JavaScript.if

//no todo IF  debe tener un else

if (hour < 18) {
    greeting = "Good day";
  }


  // CONDICIONALES
 const age = 5

 if(age >= 18) {
    console.log
 }
/*
  Utilizando la sentencia IF, escribe el código necesario para que dado un número, imprima por consola si el número es par o no.
*/
const a = 50;

if(a % 2 === 0) {
    console.log('es par')
} else {
    console.log('es impar')
}
/*
  Utilizando la sentencia IF, escribe el código necesario para que, según el valor de un número, imprima por consola:

  1, si el valor es mayor que cero,
  -1, si es menor que cero,
  0, si es igual a cero.
*/

